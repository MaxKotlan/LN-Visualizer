import { LogToLinearPipe } from './log-to-linear.pipe';

describe('LogToLinearPipe', () => {
  it('create an instance', () => {
    const pipe = new LogToLinearPipe();
    expect(pipe).toBeTruthy();
  });
});
