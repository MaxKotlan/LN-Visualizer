import { createAction } from '@ngrx/store';

export const loadGraphFromStorage = createAction('[graph-database] loadGraphFromStorage');

export const deleteGraphDatabase = createAction('[graph-database] deleteGraphDatabase');
